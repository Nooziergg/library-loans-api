namespace LibraryLoans.Domain;

/// <summary>
/// Anchors reflection over the Domain assembly (architecture tests today, EF configuration
/// discovery later). Deliberately empty: this assembly is meant to stay dependency-free.
/// </summary>
public sealed class DomainAssemblyMarker
{
}
